name = "spearyx-app-web"
compatibility_date = "2025-11-05"
compatibility_flags = [ "nodejs_compat" ]

# Note: main field removed - TanStack Start with cloudflare-pages preset handles this

[observability]
enabled = true

[[d1_databases]]
binding = "DB"
database_name = "spearyx-jobs"
database_id = "991bbe3f-8b3a-4ce4-9de8-c2110052662f"
preview_database_id = "local-spearyx-jobs"
migrations_dir = "drizzle"

# Vite/Cloudflare plugin emits a build; use `main` defined at the top and `assets` here

[assets]
directory = "dist/client"
